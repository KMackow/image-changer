const columns = {
    float: "left",
    width: "20%",
}

const columnsHover = {
    float: "left",
    width: "20%",
    opacity: "0.4",
    filter: "alpha(opacity=40)"
}

export const styles = {
    columns: columns,
    columnsHover: columnsHover
}